URL Configuration: Maps URLs to views in Django.

DefaultRouter: Automatically creates routes for the viewset, handling standard API operations.

router.register(r'companies', CompanyViewSet): Registers the CompanyViewSet with the router, creating the endpoints for CRUD operations on Company objects.

urlpatterns: Includes the routes generated by the router. This means accessing /companies/ will use the CompanyViewSet.

